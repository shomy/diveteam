<% if user_signed_in? && current_user.id === @user.id %>
  <h1>マイページ</h1>
<% else %>
  <h1>ユーザー詳細</h1>
<% end %>


<%= @user.name %>

<% if user_signed_in? && current_user.id === @user.id %>
    <%= link_to '編集', edit_user_registration_path(current_user) %>
<% end %>
